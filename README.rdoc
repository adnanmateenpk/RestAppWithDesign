== Documentation

Following are the steps you must take to initialize the project

Things you may want to cover:

* Create database.yml file 

* Add sql values 

* do these commands bundle install, rake db:create, rake db:migrate, rake db:seed

Note: always do first and last two commands after every pull