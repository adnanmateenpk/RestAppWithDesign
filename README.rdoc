== Documentation

Following are the steps you must take to initialize the project

Things you may want to cover:

* Create database.yml file 

* Add sql values 

* do these commands bundle install, rake db:create, rake db:migrate, rake db:seed , rake assets:precompile

Note: always do first and last three commands after every pull