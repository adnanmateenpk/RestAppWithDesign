<% if object.size > 0 %>
  <div id="errorExplanation">
    <h2><%= pluralize(object.size, "error") %> prohibited this record from being saved</h2>
    <ul>
    <% object.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>
